$('#sur57').on('shown.bs.modal', function () {
    $('#videosur').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#sur57').on('hidden.bs.modal', function () {
$('#videosur').get(0).pause(); // Pausa el video al cerrar el modal
});

$('#santa_rosa').on('shown.bs.modal', function () {
    $('#videosantarosa').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#santa_rosa').on('hidden.bs.modal', function () {
$('#videosantarosa').get(0).pause(); // Pausa el video al cerrar el modal
});

$('#pedro_escobedo').on('shown.bs.modal', function () {
    $('#videopedroescobedo').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#pedro_escobedo').on('hidden.bs.modal', function () {
$('#videopedroescobedo').get(0).pause(); // Pausa el video al cerrar el modal
});


$('#calamanda').on('shown.bs.modal', function () {
    $('#videocalamanda').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#calamanda').on('hidden.bs.modal', function () {
$('#videocalamanda').get(0).pause(); // Pausa el video al cerrar el modal
});



$('#celta').on('shown.bs.modal', function () {
    $('#videocelta').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#celta').on('hidden.bs.modal', function () {
$('#videocelta').get(0).pause(); // Pausa el video al cerrar el modal
});


$('#beta').on('shown.bs.modal', function () {
    $('#videobeta').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#beta').on('hidden.bs.modal', function () {
$('#videobeta').get(0).pause(); // Pausa el video al cerrar el modal
});

$('#gamma').on('shown.bs.modal', function () {
    $('#videogamma').get(0).play(); // Reproduce el video al abrir el modal
  });

$('#gamma').on('hidden.bs.modal', function () {
$('#videogamma').get(0).pause(); // Pausa el video al cerrar el modal
});

